syntax = "proto3";

package Common.Proto.Player;

enum EnterStatus {
    OK = 0;
    ERROR = 1;
}

message PlayerEnterRequest {
    int32 test = 1;
}

message PlayerEnterResponse {
    EnterStatus status = 1;
    string message = 2;
}