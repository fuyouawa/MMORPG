syntax = "proto3";

package Common.Proto.Entity;
import "Entity/NetEntity.proto";

enum EntityStatus {
    NODE = 0;
    IDLE = 1;
    MOVE = 2;
    JUMP = 3;
}

message EntitySync {
    EntityStatus status = 1;
    NetEntity entity = 2;
}